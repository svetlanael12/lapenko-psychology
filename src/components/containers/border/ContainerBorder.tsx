import { Colors } from "@/types/colors";
import styled from "@emotion/styled";

export const ContainerBorder = styled.div`
  border: 3px solid ${Colors.Coffee};
  border-radius: 5px;
  padding: 12px;

  text-align: center;
`;
